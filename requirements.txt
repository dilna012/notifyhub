# Core
Django>=5.2,<6.0
asgiref>=3.8
sqlparse>=0.5

# API / HTTP
requests>=2.31
httpx>=0.27

# Security / utils
cryptography>=42.0
cffi>=1.16
charset-normalizer>=3.3
idna>=3.7
urllib3>=2.0

# Data handling (if used in project)
numpy>=2.0
pandas>=2.2

# ML (only if your project actually uses it)
scikit-learn>=1.5
scipy>=1.13

# Optional but common
Pillow>=10.0
python-dotenv>=1.0
